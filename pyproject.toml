[project]
name = "hakken"
version = "0.1.0"
description = "hakken: deep research agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.64.0",
    "dotenv>=0.9.9",
    "tavily-python>=0.7.11",
]
